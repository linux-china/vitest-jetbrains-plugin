import {describe, expect, it, test} from 'vitest'

test('demo', () => {
    expect(Math.sqrt(2)).toBe(Math.SQRT2)
})

describe('suite', () => {
    it('demo2', () => {
        console.log("demo2")
    })
})

